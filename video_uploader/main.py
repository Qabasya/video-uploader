"""Composition root сервиса.

Этап 10: сборка зависимостей из ``Settings``, фоновый воркер сканирования,
uvicorn для HTTP-API и graceful shutdown по SIGTERM.
"""


def main() -> None:
    """Точка входа CLI ``video-uploader`` (заглушка этапа 1)."""
    raise NotImplementedError("fs-video-uploader: composition root реализуется на этапе 10")
